import React, { useState } from 'react';
import './Contact.css';

export default function Contact() {
	const [messageSent, setMessageSent] = useState(false);

	return (
		<section className="page contact-page">
			<header className="page__header">
				<h1 className="page__title">Contact & Assistance</h1>
				<p className="page__subtitle">Formulaire d'aide technique/commerciale avec zone dédiée au chatbot support.</p>
			</header>

			<div className="contact-layout">
				<form className="contact-form" action="#">
					<input className="input" placeholder="Nom" />
					<input className="input" placeholder="Email" />
					<select className="select" defaultValue="support">
						<option value="support">Support technique</option>
						<option value="commercial">Demande commerciale</option>
						<option value="commande">Suivi de commande</option>
					</select>
					<textarea className="textarea" rows="6" placeholder="Votre message" />
					<button className="btn btn--primary" type="button" onClick={() => setMessageSent(true)}>Envoyer</button>
					{messageSent ? <div className="notice notice--success">Message prêt à être transmis au backend de messagerie.</div> : null}
				</form>

				<aside className="chatbot-placeholder">
					<h3>Zone Chatbot support (à intégrer)</h3>
					<p>Ici sera ajouté l'assistant pour guider les clients sur produits, commandes et SAV.</p>
					<div className="stack">
						<div className="panel">Questions fréquentes : livraison, disponibilité, installation.</div>
						<div className="panel">Prévoir branchement à une base FAQ ou à un agent conversationnel.</div>
					</div>
				</aside>
			</div>
		</section>
	);
}
