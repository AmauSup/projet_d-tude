import React from 'react';
import './Footer.css';

export default function Footer({ onNavigate }) {
	return (
		<footer className="site-footer">
			<div className="site-footer__container">
				<div>
					<strong>Althea Systems</strong> — Plateforme e-commerce d'équipements médicaux pour cabinets, cliniques et équipes terrain.
				</div>
				<div className="site-footer__links">
					<button type="button" onClick={() => onNavigate('/contact')}>Contact</button>
					<button type="button" onClick={() => onNavigate('/account')}>Mon compte</button>
					<button type="button" onClick={() => onNavigate('/admin')}>Back-office</button>
				</div>
				<div>© {new Date().getFullYear()} Althea Systems</div>
			</div>
		</footer>
	);
}
