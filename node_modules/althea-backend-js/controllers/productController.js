const { patchProductAction } = require('./productActionController');
const { deleteProduct } = require('./productDeleteController');
const { createProduct, updateProduct } = require('./productMutationController');
const {
  getProductBySlug,
  listProducts,
  listProductsByCategory,
  searchCatalog,
  searchProducts,
} = require('./productReadController');

module.exports = {
  createProduct,
  deleteProduct,
  getProductBySlug,
  listProducts,
  listProductsByCategory,
  patchProductAction,
  searchCatalog,
  searchProducts,
  updateProduct,
};
